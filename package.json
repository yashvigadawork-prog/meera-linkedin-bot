{
  "name": "meera-linkedin-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Telegram bot that turns Meera's voice notes into draft-only LinkedIn posts in her voice.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "set-webhook": "node scripts/set-webhook.js",
    "delete-webhook": "node scripts/delete-webhook.js"
  }
}
